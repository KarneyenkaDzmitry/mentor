Привет. Подскажи, пожалуйста, такой вопрос:
Если я работая на своеё машине через порт который коннектиться к контейнеру (твоему обучающему курсу localhost:8081), запускаю другие контеёнеры, то они запускаются как отдельные контейнеры на моём хосте (localhost) или по принципу контейнер в контейнере? т.е. для них хост будет тот контейнер, через который я их запускал?
Извини, если глупый вопрос.

ААА. По моему до меня дошло. Я просто на localhost:10081 и localhost:10082 
видел результат работы такойже как и во вкладках в обучающем приложении. 
А потом покопался в docker.yaml и там нашёл, что эти порты, грубо говоря, 
маппятся на localhost (expose) и поэтому я имею доступ к двум внутренним
 контейнерам доступ по этим портам. Я не проверял, но скорее всего если этоти
  параметры удалить в docker.yaml file,
 то и не смогу на своём localhost иметь к ним доступ. 



Имеет ли свой хост Docker container внутри для того чтобы к нему обратиться?
 
Task Images unit-3:
В какой папке создавать Dockerfile?


Путаница с терминами и с ключами командной строки

registry vs repository, bind mount vs volume (оба могу быть созданы через docker run ... -v, хотя по факту это разные сущности),
 tag vs image name, EXPOSE vs expose vs ports, различные типы volumes (анонимные, именованные...). 
 И пр. В принципе, это понятно, т.к. продукт достаточно свежий и терминология в принципе не могла успеть устояться. 
 Я уж не говорю про то, что лучше все всегда смотреть в оригинальном написании,
  т.к. наши переводчики умудряются перевести так, что изначальный смысл ускользает.



  cat sample | grep -v a | sort - r

Summary:
Pipes '|' send the output of one command as input of another command.
The Filter takes input from one command, does some processing, and gives output.
The grep command can be used to find strings and values in a text document
Piping through grep has to be one of the most common uses
'sort' command sorts out the content of a file alphabetically
less ,pg and more commands are used for dividing a long file into readable bits



Не хватает кнопки для перезагрузки контейнера или перехода/выбора другого тренингаю Приходиться постоянно restart делать
Если нет градации по верным и не верным ответам в квизе или сделанным или нет пунктам задания, то лучше использовать графики в саммари другого формата - типа pass/fail/skip
А затем использовать процент пройденых и выщитывать процент. Пример планка выше 80% прошёл на отлично, 100% прошёл великолепни и т.д.


compose task description add space in docker command script
